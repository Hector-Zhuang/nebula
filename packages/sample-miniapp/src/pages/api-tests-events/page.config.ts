import { definePageConfig } from '@nebula-rn/sdk';

export default definePageConfig({
  route: '/api-tests-events',
  backgroundColor: '#ecfeff',
  navigationBarBackgroundColor: '#cffafe',
  navigationBarTextColor: '#155e75',
  navigationBarTitleText: 'Events & Sensors',
  navigationStyle: 'default',
  visualEffectInBackground: 'blur',
});
