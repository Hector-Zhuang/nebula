import { definePageConfig } from '@nebula-rn/sdk';

export default definePageConfig({
  route: '/api-tests-device',
  backgroundColor: '#ffffff',
  navigationBarBackgroundColor: '#dbeafe',
  navigationBarTextColor: '#1e3a8a',
  navigationBarTitleText: 'Device APIs',
  navigationStyle: 'default',
  visualEffectInBackground: 'blur',
});
